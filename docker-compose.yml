services:
    postgres:
        image: postgres:16-alpine
        container_name: technicaltask-postgres
        restart: unless-stopped
        volumes:
            - postgres_data:/var/lib/postgresql/data
        ports:
            - "5432:5432"
        environment:
            POSTGRES_DB: ${DATABASE_NAME:-technicaltask_db}
            POSTGRES_USER: ${DATABASE_USER:-postgres}
            POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-postgres}
            POSTGRES_HOST_AUTH_METHOD: md5


    adminer:
        image: adminer:latest
        container_name: technicaltask-adminer
        restart: unless-stopped
        ports:
            - "8080:8080"
        depends_on:
            - postgres


# api: nest js local container not yet implemented ( pnpm run test & pnpm run seed scripts should be modified )
    #     build: .
    #     container_name: technicaltask-api
    #     restart: unless-stopped
    #     ports:
    #         - "3000:3000"
    #     environment: # For Type ORM must Match Database ( refer to .env.example )
    #         DATABASE_HOST: 'postgres'
    #         DATABASE_PORT: '5432'
    #         DATABASE_NAME: ${DATABASE_NAME:-technicaltask_db}
    #         DATABASE_USER: ${DATABASE_USER:-postgres}
    #         DATABASE_PASSWORD: ${DATABASE_PASSWORD:-postgres}
    #         CLIENT_URL: ${CLIENT_URL:-http://localhost:3000}  # CORS URL 
        
    #     depends_on:
    #         - postgres


volumes:
  postgres_data:
    driver: local